const Webhook = require('./classes/webhook');
const MessageBuilder = require('./classes/messageBuilder');

module.exports = {
    default: {
        Webhook,
        MessageBuilder
    },
    Webhook,
    MessageBuilder
};
